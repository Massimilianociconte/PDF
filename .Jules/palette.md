## 2024-05-22 - Login Form Accessibility
**Learning:** Adding `htmlFor` and matching `id`s to labels is the single most impactful a11y fix for forms, as it enables screen reader association and click-to-focus.
**Action:** Always verify form inputs have associated labels using `htmlFor` and `id` pairs during initial component creation.
