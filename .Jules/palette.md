## 2024-05-23 - Accessibility in Auth Forms
**Learning:** Even simple login forms often miss basic accessibility attributes like `htmlFor` and `id` linking, and `role="alert"` for errors.
**Action:** Always verify form accessibility with screen readers or linting tools that check for label associations.
